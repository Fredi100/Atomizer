package emsenhuber.alfred.atomizer;

import emsenhuber.alfred.atomizer.lib.Constants;
import net.minecraft.client.gui.GuiScreen;
import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.util.ResourceLocation;
import net.minecraft.world.World;

public class GuiAssembler1 extends GuiScreen{
	/*
	private int x, y, z, xSize, ySize;
	private World world;
	private EntityPlayer player;
	private ResourceLocation backgroundimage = new ResourceLocation(Constants.MODID.toLowerCase() + ":" + "texture/gui/GuiAssembler1.png");
	
	public GuiAssembler1(EntityPlayer player, World world, int x, int y, int z){
		this.x = x;
		this.y = y;
		this.z = z;
		this.world = world;
		this.player = player;
		xSize = 176;
		ySize = 214;
	}
	
	@Override
	public void drawScreen(int mouseX, int mouseY, float renderPartialTicks){
		this.mc.getTextureManager().bindTexture(backgroundimage);
		int x = (this.width - xSize) / 2;
		int y = (this.height - ySize) / 2;
		drawTexturedModalRect(x, y, 0, 0, xSize, ySize);
	}
	
	@Override
	public boolean doesGuiPauseGame(){
		return false;
	}
	*/
}
